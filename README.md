This is a simple machine learning project demonstrating linear regression using `scikit-learn`.  
It generates synthetic data, trains a small regression model, and visualizes the regression line against the actual data points.
